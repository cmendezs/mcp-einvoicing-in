# mcp-einvoicing-in has no required environment variables (Phase A only —
# no network calls). Live IRP submission (Phase B) will require IRP/GSP
# credentials once the NIC API spec is staged; this file will be updated
# at that time.
